{
  "compilerOptions": {
    /* Salida de info de compilación (cache) */
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",

    /* Objetivo y librerías */
    "target": "ES2022",
    "lib": ["ES2023"],

    /* Módulos */
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",

    /* Aceptar y **no** checar archivos .js → evita la advertencia */
    "allowJs": true,
    "checkJs": false,

    /* Salida */
    "noEmit": true,
    "skipLibCheck": true,

    /* Reglas de calidad */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },

  /* Solo necesitamos que vea la config de Vite */
  "include": ["vite.config.js"]
}
